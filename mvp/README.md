# GameDebate MVP

Projet de plateforme sociale de débat vidéoludique.

## 📁 Structure
- `/docs` : Documentation Produit, UX, Specs et Tech.
- `/game-debate-app` : Le code source du MVP (React/Vite).

## 🚀 Lancement Rapide

1. **Aller dans le dossier**
   ```bash
   cd game-debate-app
   ```

2. **Installer les dépendances** (si ce n'est pas fait)
   ```bash
   npm install
   ```

3. **Lancer le serveur de dev**
   ```bash
   npm run dev
   ```

4. **Ouvrir le navigateur**
   Rendez-vous sur l'URL indiquée (généralement `http://localhost:5173`).

## 🛠️ Stack Technique
- **Frontend** : React
- **Styling** : TailwindCSS + Framer Motion
- **Icons** : Lucide React
- **Data** : Service Mocké (fichier JSON statique)
